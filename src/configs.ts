export default {
  /*
     *PayAttention=> times is in ms.
     */
  authorizationCodeAge: 100000,
  accessTokenAge: 300000,
  refreshedAccessTokenAge: 300000,
  refreshTokenAge: 600000,
  refreshTokenAvailable: true,
}
